{{#with footer}}
<footer class="footer">
    <div class="footer_links">
        {{#each pages}}
        <a href="{{path}}" class="footer_link">{{name}}</a>
        {{/each}}
    </div>
    <p class="footer_copyright">{{copyright}}</p>
</footer>
{{/with}}